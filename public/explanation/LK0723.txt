**正确选项解释**：
发光二极管（LED）必须在正向偏置下连接才能发光，但直接连接到直流电源会导致电流过大而损坏，因此需串联限流电阻以控制电流。

**错误选项解释**：
 - 选项B：反向连接LED不会发光，且可能因反向电压击穿而损坏，串联电阻无法防止此问题。
 - 选项C：正向连接是必需的，但限流电阻必须串联而非并联；并联电阻不能有效限流，反而可能分流电流导致保护失效。
 - 选项D：正向直接连接会导致过流，迅速烧毁LED。

**关键字**：
发光二极管、正向连接、串联限流电阻