**正确选项解释**：
当输出功率是输入功率的一半时，功率增益在分贝系统中计算为 -3dB，这是一个标准值，因此选项A正确。

**错误选项解释**：
- 选项B: -1dB 对应输出功率约为输入的79%，不是一半。
- 选项C: -2dB 对应输出功率约为输入的63%，不是一半。
- 选项D: 0.5dB 是正值，表示功率增益（输出增加约12%），而题目中输出小于输入。

**关键字**：
输出功率是输入功率的1/2