**正确选项解释**：
选项A：APRS（Automatic Packet Reporting System）是一种基于数据包的通信系统，常用于2米业余波段。其主要用途是让移动中的业余电台通过GPS获取地理位置信息，并将其发送出去，实现位置共享和相关信息的实时传输。

**错误选项解释**：
 - 选项B：APRS并不用于传输高清视频，其数据速率和协议不适合此类高带宽应用。
 - 选项C：虽然APRS可用于应急通信，但其主要用途不是专门用于守听突发事件的安排。
 - 选项D：APRS采用的是数据包调制方式，而非线性调频方式，也不主要用于发送文字短消息。