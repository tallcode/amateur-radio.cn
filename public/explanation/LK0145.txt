**正确选项解释**：
选项A列出了1.8MHz（160米波段）、3.5MHz（80米波段）、14.25MHz（20米波段）、18.068MHz（17米波段）、24.89MHz（12米波段）。这些频段在中国业余无线电频率划分中（依据《中华人民共和国无线电频率划分规定》），业余业务和卫星业余业务作为主要业务之一，与其他业务共用，且均在30MHz以下。具体频段分配为：1.8-2.0MHz（160m）、3.5-3.9MHz（80m）、14.0-14.35MHz（20m）、18.068-18.168MHz（17m）、24.89-24.99MHz（12m），所列频率点均代表各波段起始或标准参考点，符合中国法规。

**错误选项解释**：
 - 选项B：列出了3.5MHz（80m）、7MHz（40m）、14.25MHz（20m）、21MHz（15m）、24.89MHz（12m），但缺少17米波段（18.068MHz）。17米波段在中国是分配的且业余业务为主要业务，此选项不完整。
 - 选项C：列出了3.5MHz（80m）、10.1MHz（30m）、14.25MHz（20m）、18.068MHz（17m）、29.7MHz（10m）。29.7MHz是10米波段（28.0-29.7MHz）的上限频率，并非波段起始或标准参考点；同时，此列表缺少12米波段（24.89MHz），且29.7MHz的表示方式不典型。
 - 选项D：列出了1.8MHz（160m）、10.1MHz（30m）、14.25MHz（20m）、18.068MHz（17m）、21.45MHz（15m）。21.45MHz是15米波段（21.0-21.45MHz）的上限频率，并非起始点；同时，此列表缺少12米波段（24.89MHz），不完整。