**正确选项解释**：
选项A正确，因为具有多种带宽选择的接收机可以根据不同的通联方式（如CW、SSB、FM、数字模式等）选择最合适的接收带宽，从而有效提高信噪比，提升接收效果。

**错误选项解释**：
- 选项B描述的是多个接收机的协同工作，与单个接收机的带宽选择无关。
- 选项C错误，因为一个接收机在同一时间只能接收一种信号，不能通过带宽选择实现多信号同时接收。
- 选项D虽然涉及多接收机操作，但并不是“多种带宽选择”这一功能本身的优点，而是系统配置的问题。