**正确选项解释**：
选项A：MODE功能通常用于切换收发信机的工作模式，如FM（调频）、LSB（下边带）、USB（上边带）和CW（连续波，用于摩尔斯电码通信）等，这是国际通用的标准操作。

**错误选项解释**：
- 选项B：切换静噪方式（如CTSS和DCS）通常是通过“SQL”或“TONE”功能实现，而不是MODE。
- 选项C：接收机的硬件工作方式（如射频直采或超外差）是设备内部结构设计决定的，一般不会通过面板上的MODE键切换。
- 选项D：监听方式如单耳、双耳或立体声是音频输出设置，通常由音频菜单或专门的监听键控制，与MODE无关。