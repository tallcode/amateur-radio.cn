**正确选项解释**：
选项A：每次CK上升沿到来时，Q端都变得与D端一致。D触发器在时钟上升沿触发时，会将Q端的状态更新为D端的值，这是D触发器的基本工作原理。

**错误选项解释**：
- 选项B：Q端与D端反相是D触发器的变种（如反相输出D触发器）才可能出现的情况，但题目未特别说明，默认情况下Q端与D端同相。
- 选项C：Q端在CK下降沿变化且反相，这不符合上升沿触发的D触发器定义，更像是其他类型的触发器或边沿触发方式的误解。
- 选项D：Q端在CK下降沿变化且与D端一致，这与题目中“上升沿触发”的设定不符，属于触发沿判断错误。