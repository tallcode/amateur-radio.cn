**正确选项解释**：
- 选项A正确。ITA2 编码也被称为博多码（Baudot code），在业余无线电中广泛用于 RTTY（无线电传）通信。它是一种 5 位二进制编码，通过换态扩位技术可以表示更多的字符。

**错误选项解释**：
- 选项B错误。PSK31 通信使用的编码不是 ITA2，而是基于 ASCII 的变长编码方案，如 Varicode。
- 选项C错误。PACKET 通信（如 APRS）使用的是基于 8 位字节的协议（例如 ASCII），而不是 ITA2。
- 选项D错误。莫尔斯电码是 CW 模式使用的编码，属于可变长度编码，但它与 ITA2 无关。