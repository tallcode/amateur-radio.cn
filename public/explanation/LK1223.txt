**正确选项解释**:
选项A：天线与馈线的连接头接触不良会导致阻抗不匹配，引起反射功率波动，从而使驻波比（SWR）读数不稳定。

**错误选项解释**:
- 选项B：频率调制是信号调制方式，不影响SWR测量，因为SWR取决于天线系统的阻抗匹配，与调制类型无关。
- 选项C：发射机过调制可能导致信号失真，但不会直接影响SWR读数，因为SWR是硬件匹配问题而非信号处理。
- 选项D：来自其他电台的干扰主要影响接收端信号，而SWR测量的是发射端反射功率，因此干扰不会导致SWR不稳定。

**关键字**:
连接头接触不良, 驻波比不稳定