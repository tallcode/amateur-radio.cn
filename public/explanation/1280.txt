**正确选项解释**：
选项A（1、0、0、0）是正确的。或非门（NOR）的逻辑功能是：只有当所有输入都为0时，输出才为1；否则输出为0。因此，输入组合00对应输出1，其余组合01、10、11均对应输出0。

**错误选项解释**：
 - 选项B（0、1、1、1）是错误的，因为它表示在输入01、10、11时输出为1，这与或非门的功能相悖。
 - 选项C（0、1、0、1）和选项D（1、0、1、0）也都是错误的，因为它们没有满足或非门仅在输入全为0时输出为1的逻辑规则。