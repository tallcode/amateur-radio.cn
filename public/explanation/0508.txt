**正确选项解释**：
- 选项A：FT8 是一种高效的数字通信模式，具有很强的弱信号解码能力，因此即使使用小功率发射，也能实现远距离（DX）通信。
- 选项B：FT8 的弱信号特性使其适用于探测大气波导、流星余迹和对流层散射等传播现象，这些现象可以支持非视距和远距离通信。

**错误选项解释**：
- 选项C：FT8 的带宽约为 50Hz，而不是 3kHz，且其信息结构紧凑，不是以冗余为基础。
- 选项D：FT8 的符号速率约为 2.7 baud（不是 50bps），并且使用特定编码结构，不是 5 单位博多码。