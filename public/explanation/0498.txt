**正确选项解释**：
- 选项A：RTTY（无线电电传）是一种典型的异步通信方式，它使用固定的字符长度和起始位、停止位来实现每个字符的独立同步，不要求发送端和接收端在每个符号上严格同步。

**错误选项解释**：
- 选项B：QPSK31 是一种同步数字通信模式，需要收发双方在帧结构和时钟上保持同步。
- 选项C：PACKET（特别是AX.25协议）虽然在某些实现中可能有异步特征，但本质上是基于帧的同步通信方式。
- 选项D：PACTOR-II 是一种高性能的同步通信协议，依赖于严格的时序和帧同步来实现数据传输。