**正确选项解释**：
符号速率（Baud）是每秒传输的符号数。每个符号持续时间为 64 毫秒，即 0.064 秒，因此符号速率为 1 / 0.064 = 15.625 波特。
每个符号表示 4 位信息（因为 2⁴ = 16），所以数据速率为 15.625 × 4 = 62.5 比特/秒。

**错误选项解释**：
- 选项B：数据速率计算错误，误将每个符号位数算作 16。
- 选项C：误将数据速率等同于符号速率，忽略了每个符号携带的信息量。
- 选项D：符号速率计算错误，且数据速率也错误。