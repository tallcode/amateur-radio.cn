**正确选项解释**：噪声系数Fn定义为接收机输入端与输出端信噪比的比值，实际接收机中Fn总是大于或等于1（理想值为1）。在同样的灵敏度下，Fn越接近1，表示接收机内部噪声越低，能更好地接收微弱信号。

**错误选项解释**：
 - 选项B：Fn一定小于1是错误的（因为Fn≥1），且Fn越接近于0越好也是错误的（最小值为1）。
 - 选项C：Fn一定大于1是正确的，但Fn越大越好是错误的（较高的Fn表示更多内部噪声，性能更差）。
 - 选项D：Fn一定小于1是错误的（因为Fn≥1）。

**关键字**：噪声系数Fn, 大于1, 越接近于1越好