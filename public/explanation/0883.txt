**正确选项解释**：
选项A正确，因为接收机的噪声指数NF是以对数表示的系统内部噪声的度量，其值一定大于0。NF越接近0，说明接收机引入的噪声越少，性能越好。

**错误选项解释**：
- 选项B错误，因为NF不可能小于0，接收机的噪声指数总是正数。
- 选项C错误，因为NF不一定大于1，它可以在0到1之间（以线性值表示时），对应的对数形式为正值。
- 选项D错误，因为NF以对数形式表示时通常为正值，不会限制在0和1之间。