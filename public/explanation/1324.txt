**正确选项解释**：
输入功率是输出功率的一半，因此输出功率是输入功率的两倍（P_out / P_in = 2）。功率增益公式为 Gain(dB) = 10 × log10(P_out / P_in)，代入得 10 × log10(2) ≈ 3dB。

**错误选项解释**：
 - B: -3dB 表示功率损耗（输出小于输入），但本题输出大于输入，因此不正确。
 - C: 0.5dB 对应功率比约为 1.122（即输出是输入的约 1.122 倍），而实际为 2 倍，不匹配。
 - D: -0.5dB 表示轻微损耗（功率比约为 0.891），与实际增益不符。