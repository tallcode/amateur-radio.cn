**正确选项解释**：
选项A中，FM的调制方式在无语音输入时，载波功率保持不变，因此射频输出功率最大；AM在无调制时有载波输出，但功率低于FM；SSB在无语音输入时几乎不发射功率，因此实际射频输出功率由大到小为FM，AM，SSB。

**错误选项解释**：
- 选项B中AM的功率高于SSB是错误的，因为SSB在无语音时几乎不发射功率。
- 选项C中SSB功率最高不符合实际，因为SSB在无语音时几乎没有输出。
- 选项D中SSB功率最高也是错误的，因为SSB依赖语音信号发射功率。