**正确选项解释**：
SQL代表静噪控制（Squelch Control），其功能是当接收信号强度低于设定阈值时，自动关闭音频输出，以避免背景噪声干扰。

**错误选项解释**：
- 选项B：描述的是语音压缩功能（如Compressor），用于发射机提升语音动态范围，与SQL无关。
- 选项C：描述的是前置放大器（Pre-amplifier），用于增强微弱信号接收，不是SQL的功能。
- 选项D：描述的是自动天线调谐器（ATU），用于优化天线匹配，与SQL无关。

**关键字**：
SQL