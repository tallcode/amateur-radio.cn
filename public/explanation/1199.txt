**正确选项解释**：
- 选项A：A类放大器确实是在整个信号周期内导通，工作在线性区域，既不截止也不饱和，因此失真小但效率低。
- 选项B：B类放大器的有源器件只在半个周期内导通，其余时间截止，且不进入饱和，用于提高效率，但存在交越失真。
- 选项C：C类放大器导通角小于180度，通常用于高频放大，器件在峰值时进入饱和状态，效率高但失真大。

**错误选项解释**：
- 选项D：D类放大器不是基于模拟线性工作，而是通过开关方式工作，器件在导通和截止之间切换，几乎不在线性区工作，因此“始终线性工作”是错误的。