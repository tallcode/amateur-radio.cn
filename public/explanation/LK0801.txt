**正确选项解释**:
业余无线电慢扫描电视（SSTV）标准使用调频（FM）调制，其中白电平（高亮度）对应高频率（约2300Hz），黑电平（低亮度）对应低频率（约1500Hz），同步电平对应最低频率（约1200Hz）。

**错误选项解释**:
 - B: 调制方式错误（应为调频而非调幅），电平值描述不标准。
 - C: 调制方式错误（应为调频），且白和黑电平相同（75%）不逻辑（亮度应不同）。
 - D: 调制方式错误（应为调频），描述频率但调制为调幅不一致；频率分配反了（白电平应为高频率）。

**关键字**:
调频、调幅、白电平、黑电平、同步电平