**正确选项解释**：
- 选项A：驻波比（SWR）确实是用来描述负载阻抗与传输线特性阻抗匹配程度的一个参数，匹配越好，SWR越接近1。
- 选项B：当负载完全匹配传输线特性阻抗时，不会产生驻波，此时驻波比为1:1，这是SWR的最小值，表示理想匹配状态。
- 选项C：当负载不匹配时，确实会导致一部分能量反射回传输线的始端，形成驻波现象。

**错误选项解释**：
- 选项D：驻波是由负载与传输线之间的不匹配引起的，仅通过调整信源阻抗无法消除传输线中的驻波，因此该说法错误。