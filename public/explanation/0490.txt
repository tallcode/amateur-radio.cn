**正确选项解释**：
选项A：FSK（频移键控）是RTTY常用的数字调制方式，用于将数字信息转换为音频基带信号；SSB（单边带调制）用于将音频信号调制到无线电频率上进行传输，符合RTTY通联的常规操作。

**错误选项解释**：
 - 选项B：MT63是一种用于数字通信的模式，但不是RTTY的一部分，它通常用于弱信号条件下的文本通信。
 - 选项C：DRM是一种数字广播模式，用于短波广播，不是RTTY的标准调制方式。
 - 选项D：RTTY不仅需要SSB，还需要基带调制（如FSK）来传输数字信息，因此仅使用SSB是不完整的。