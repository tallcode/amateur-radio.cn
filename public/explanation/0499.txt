**正确选项解释**:
- 选项A: BPSK31是一种同步通信方式，因为它使用连续相位调制，没有起始/停止位，要求严格的时钟同步来解码每个符号。
- 选项B: APRS基于AX.25协议，该协议使用同步数据帧（如HDLC），需要收发端时钟在帧内保持同步。
- 选项C: FT8是同步模式，依赖于精确的时间同步（如GPS），每个符号在固定时间间隔传输，要求严格同步。

**错误选项解释**:
- 选项D: RTTY是异步通信方式，每个字符独立发送，包含起始和停止位，允许时钟有轻微漂移，不需要严格同步。