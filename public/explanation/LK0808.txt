**正确选项解释**:
RTTY（无线电传打字机）使用起始位和停止位传输每个字符，时钟同步仅在单个字符（字节）传输期间保持，因此属于异步方式。

**错误选项解释**:
 - PSK31（误写为QPSK31，实际指PSK31）采用连续相位调制，需要在整个数据块传输期间时钟同步，属于同步方式。
 - PACKET（如AX.25协议）以帧结构传输数据，依赖块级时钟同步，属于同步方式。
 - PACTOR-II使用高级调制和纠错，需要整个数据块期间的时钟同步，属于同步方式。

**关键字**:
字节的时间内, 一整块数据的时间内