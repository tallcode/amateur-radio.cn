**正确选项解释**：
符号速率（Baud）是每秒传输的符号数。每个符号持续32毫秒，即0.032秒，因此每秒可传输1 / 0.032 = 31.25个符号，即符号速率为31.25波特。
QPSK使用4种相位状态（00、01、10、11），每个符号携带2比特信息，因此数据速率为31.25 × 2 = 62.5比特/秒。

**错误选项解释**：
- 选项B错误地认为每个符号只携带1比特信息，而QPSK实际携带2比特。
- 选项C中的符号速率和数据速率均不符合计算结果。
- 选项D中数据速率为125比特/秒对应每个符号携带4比特，这不符合QPSK调制方式。