**正确选项解释**：
与非门（NAND）的逻辑功能是先进行与运算，然后取非。其真值表为：当两个输入都为1时，输出为0；其余情况，输出均为1。
因此，对于输入组合00、01、10、11，对应的输出为1、1、1、0。

**错误选项解释**：
- 选项B表示输出为0、1、1、1，不符合NAND门的逻辑行为。
- 选项C表示输出为0、1、0、1，这更像是异或门（XOR）的行为，而非与非门。
- 选项D表示输出为1、0、1、0，这不符合任何标准门电路的真值表。