**正确选项解释**：
 - 选项A：F2B 表示调频（F）方式下，主载波被用于传输电报（Télégraphie，B），即RTTY信号，符合题意。

**错误选项解释**：
 - 选项B：F3E 表示调频（F）方式下，发送的是话音（E），不是RTTY信号。
 - 选项C：J3E 表示单边带调制（J）传输话音（E），不用于RTTY。
 - 选项D：F3F 表示调频（F）方式下传输图像（F），与RTTY无关。