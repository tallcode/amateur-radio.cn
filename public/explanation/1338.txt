**正确选项解释**：
选项A正确：每路增益为 (x - 3) dB，因为功率分配器将输入功率等分为两路，每路相对于分配器输入损失3 dB（理想情况下）。
选项B正确：增益的倍数表示为 10^((x-3)/10) 倍，与 (x-3) dB 的增益等价。

**错误选项解释**：
- 选项C错误： (x/2) dB 不正确，因为dB增益是对数单位，不能直接线性除以2。
- 选项D错误： 10^((x/2)/10) 倍 基于C的错误计算，导致不正确的倍数增益。