**正确选项解释**：
因为增益以分贝（dB）为单位时，多个电路的总增益等于各个增益的 dB 值之和。

**错误选项解释**：
 - 选项B：dB 增益相乘是错误的，因为 dB 单位是加法的，不是乘法的。
 - 选项C：线性倍率相加是错误的，因为增益是 dB 值，直接加和不能得到正确的线性增益（实际线性倍率需要转换）。
 - 选项D：线性倍率相乘是错误的，因为 x, y, z 是 dB 值，相乘后作为线性增益不正确（实际线性倍率是各增益线性值的乘积）。

**关键字**：dB