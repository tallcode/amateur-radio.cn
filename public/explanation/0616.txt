**正确选项解释**：
选项A正确。PROC（Processing Compression）功能指的是发信语音压缩，用于对音频输入电平进行检测并反馈控制，使语音信号的动态范围被压缩，提升较小幅度语音的可听性。

**错误选项解释**：
 - 选项B描述的是收信机前置放大器的功能，与PROC无关。
 - 选项C描述的是自动天线调谐器（ATU）的功能，也不是PROC的用途。
 - 选项D描述的是发信自动电平控制（ALC），虽然与发射有关，但不是语音压缩的功能。