**正确选项解释**：
选项A中的符号速率（波特率）为50波特，表示每秒传输50个符号。由于RTTY使用两个频率分别表示0和1，每个符号对应1个比特的信息量，因此数据速率也为50比特/秒。

**错误选项解释**：
- 选项B：符号速率和数据速率均过高。RTTY的符号发送时长为0.02秒，意味着每秒只能传输1 / 0.02 = 50个符号，即50波特。
- 选项C：数据速率不应高于符号速率，因为每个符号仅携带1比特信息。
- 选项D：符号速率计算错误，应为每秒50个符号，而非100波特。