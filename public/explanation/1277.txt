**正确选项解释**：
或门（OR）的逻辑功能是：只要有一个输入为1，输出就为1。只有当所有输入都为0时，输出才为0。因此，对于输入组合00、01、10、11，对应的输出依次为0、1、1、1。

**错误选项解释**：
- 选项B表示输出为0、1、1、0，这不符合或门的逻辑行为，因为最后输入为11时输出应为1。
- 选项C表示输出为0、1、0、1，这不符合或门的逻辑行为，因为输入01和10时输出应都为1。
- 选项D表示输出为1、0、1、0，这完全不符合或门的逻辑行为，或门在输入为00时应输出0。