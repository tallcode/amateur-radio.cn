**正确选项解释**：
异或非门（NXOR）的逻辑功能是：当两个输入相同时输出为1，不同时输出为0。因此对于输入组合00（相同）输出为1，01（不同）输出为0，10（不同）输出为0，11（相同）输出为1，对应选项A。

**错误选项解释**：
- 选项B表示0、1、1、1，不符合NXOR逻辑；
- 选项C表示1、1、0、0，也不符合NXOR逻辑；
- 选项D表示0、1、1、0，同样不符合NXOR逻辑。