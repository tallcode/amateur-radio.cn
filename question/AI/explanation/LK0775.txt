**正确选项解释**：
选项A的输出序列1、0、0、1对应NXOR门（异或非门）的真值表：输入00输出1（同相），输入01输出0（反相），输入10输出0（反相），输入11输出1（同相）。

**错误选项解释**：
- 选项B：输出0、1、1、1不符合NXOR行为，可能是错误逻辑。
- 选项C：输出1、1、0、0不符合NXOR行为，类似于AND门但不是标准。
- 选项D：输出0、1、1、0是异或门（XOR）的真值表，而非异或非门（NXOR）。

**关键字**：
异或非门、NXOR