**正确选项解释**：放大器分为A、B、C、D类是基于放大器件的工作点范围（如偏置点位置），决定了导通周期和效率，例如A类工作在线性区中点。

**错误选项解释**：
 - 选项B：分类与质量等级无关，A、B、C、D不代表质量好坏。
 - 选项C：分类不基于最高工作频率，频率能力不是标准（如C类常用于高频但分类基于工作点）。
 - 选项D：分类不基于最大输出功率，功率输出取决于电路设计而非分类。

**关键字**：工作点