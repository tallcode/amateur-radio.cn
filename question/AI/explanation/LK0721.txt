**正确选项解释**：
输入-输出特性曲线直接显示放大器的静态工作点位置，例如A类工作点在线性区中心（360度导通），B类在截止点（180度导通），C类在截止区以下（小于180度导通），便于直观区分类别差异。

**错误选项解释**：
- 选项B：功率-时间曲线描述功率随时间动态变化，适用于效率分析，但不直接反映静态工作点的位置差别。
- 选项C：频率-时间曲线用于频率和时间相关分析（如频谱），与放大器静态工作点无关。
- 选项D：输入阻抗特性曲线展示输入阻抗随频率变化，虽受偏置影响，但不直接表达工作点位置的核心差别。

**关键字**：
放大器工作点，差别