**正确选项解释**:  
选项A准确描述了前向纠错（FEC）的核心原理：发送端在数据中添加冗余码，接收端通过解码算法自动检测和纠正错误，无需请求重传。

**错误选项解释**:  
- B：描述的是自动重传请求（ARQ），涉及接收端请求重传或发送端重发数据，不符合FEC定义。
- C：类似于ARQ机制，接收端反馈后发送端重传，不是FEC的无反馈纠错方式。
- D：描述的是简单重复发送方案，接收端比较选取，不是FEC基于数学编码的纠错方法。

**关键字**:  
前向纠错, FEC