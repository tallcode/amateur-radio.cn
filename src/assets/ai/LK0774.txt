**正确选项解释**：
或非门（NOR）的真值表是：当两个输入均为0时输出1，否则输出0。因此，输入00、01、10、11对应的输出依次为1、0、0、0。

**错误选项解释**：
 - 选项B: 0、1、1、1，类似于或门（OR）的输出，但或非门是或门的反转，因此错误。
 - 选项C: 0、1、0、1，类似于异或门（XOR）的输出，不匹配或非门行为。
 - 选项D: 1、0、1、0，不符合任何标准逻辑门定义，错误。

**关键字**：
或非门（NOR）