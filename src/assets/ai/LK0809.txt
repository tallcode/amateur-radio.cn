**正确选项解释**：
RTTY（无线电传打字机）使用起始位和停止位来同步每个字符，只在传送一个字节的时间内保持时钟同步，因此是异步方式，不属于同步方式。

**错误选项解释**：
- PACKET（如AX.25协议）使用帧结构和同步序列，需要在传送一整块数据（帧）的时间内保持时钟同步，因此是同步方式。
- BPSK31基于PSK31协议，采用连续相位调制，需要在整块数据传输期间保持时钟同步，因此是同步方式。
- AMTOR-FEC使用块传输和纠错编码，需要在传送一整块数据的时间内保持时钟同步，因此是同步方式。

**关键字**：
一个字节的时间, 一整块数据的时间