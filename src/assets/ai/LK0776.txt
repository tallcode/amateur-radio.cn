**正确选项解释**：
在RTTY通信中，每个码元时间长度为0.02秒，信号速率（波特率）为每秒传输的码元数，即1 / 0.02 = 50波特。每个码元代表一个比特（0或1），因此数据速率（比特率）也为50位/秒。

**错误选项解释**：
- 选项B：100波特和100位/秒错误，因为波特率计算应基于码元时间0.02秒，应为50波特而非100。
- 选项C：50波特正确，但100位/秒错误，因为每个码元只有一个比特，数据速率应为50位/秒而非100。
- 选项D：100波特错误（应为50波特），50位/秒正确但不匹配波特率。

**关键字**：
每个码元的时间长度为0.02秒